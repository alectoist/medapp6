<%= form_tag drugs_path, :method => :get do %>

<p>
	<%= text_field_tag :search, params[:search], :class => "typeahead" %>
	<%= button_tag(type: "submit", class: "btn btn-default") do %>
    Submit <i class="icon-repeat"></i>
	<% end %>
</p>

<% end %>
