<div class="results-wrap">
  
    <% @drugs.each do |drug| %>
      <div class="drug-wrap">
        <div><h3><%= link_to "#{drug.name}", drug  %>
          <p class="drug-description"><%= truncate(drug.description, length: 300) %></p></h3>
        </div>

      </div>
    <% end %>
  
</div>